# Clone dự án (Github CLI)
    gh repo clone duymanh1606/seminar
# Các bước chạy (Terminal)
1. Kích hoạt môi trường (trên terminal): 
        - uv venv
        - source .venv/bin/activate
        - uv pip install -r requirements.txt

2. Chạy code:
        - python src/pipeline.py 